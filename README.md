# Infrastructure

## Scripts

When writing scripts for this repository (such as `mise` tasks), prefer using `bun` instead of `bash` or `python` where possible. This ensures scripts are fast, natively support modern JavaScript/TypeScript, and have access to Node.js built-ins.